import React from "react";
import styled from "styled-components";

const LogIn = () => {
    return (
       <Wrapper>Log In</Wrapper>

    );
  };
  
  export default LogIn;
  


  const Wrapper = styled.div`
  `